# Angular 22 requires Node ^22.22.3 || ^24.15.0 || >=26. Pinning it here means
# `pnpm build` / `pnpm test` always run on the right Node, regardless of what
# nvm happens to have active in the current shell — pnpm fetches and caches this
# version itself.
use-node-version=24.18.1
